missing dumps
- vlan
  - no action for push and pop
  - for now, when matching vlan we can conclude for pop
  - Need to query vport attr SET_VLAN_INSERT
            esw_vport_context.vport_cvlan_insert
- vxlan
  - test L4 with vxlan (inner match ports)
  - Need to query vport attr if we do encap

- ipv6
- icmp
- counters
  - Need to query counter value. added script read_flow_counter.sh to do it.
    need to call it from the main script when asking for counters.
